{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	| Recipe
{% endblock %}

{% block body %}
	<h1>{{recipe.title}}</h1>
	<p>{{recipe.description}}</p>
	<p>{{recipe.ingredients}}</p>
	<p>{{recipe.instructions}}</p>

{% endblock %}
